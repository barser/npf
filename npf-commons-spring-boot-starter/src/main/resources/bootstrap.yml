spring:
  cloud:
    config:
      uri: ${NPF_CFG_URL:http://localhost:8888}
      username: ${NPF_CFG_USER:ospos-cs}
      password: ${NPF_CFG_PASSWORD:o$p0s123}
      fail-fast: true

---
spring:
  profiles: localconfig
  cloud:
    config:
      enabled: false

